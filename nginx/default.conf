server {
    listen 80;
    server_name localhost;

    # Serve frontend static files
    location / {
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Nginx-Proxy true;

        # Proxy to Vue.js frontend
        proxy_pass http://flasky-frontend:5173;
        proxy_redirect off;

        # WebSocket support for Vite HMR
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Proxy API requests to Flask backend
    location /api {
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Nginx-Proxy true;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Proxy to Flask backend
        proxy_pass http://flasky-backend:3000;
        proxy_redirect off;

        # Handle CORS if needed
        add_header Access-Control-Allow-Origin *;
        add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS";
        add_header Access-Control-Allow-Headers "Origin, X-Requested-With, Content-Type, Accept, Authorization";

        # Handle preflight requests
        if ($request_method = 'OPTIONS') {
            return 204;
        }
    }

    # Optional: Serve static assets directly (if you have any)
    location /static {
        proxy_pass http://flasky-backend:3000;
    }
}

# Alternative configuration if running without Docker
# Uncomment and modify the proxy_pass URLs below:
#
# server {
#     listen 80;
#     server_name localhost;
#
#     location / {
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header Host $http_host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Nginx-Proxy true;
#
#         proxy_pass http://localhost:5173;  # Vue.js frontend
#         proxy_redirect off;
#
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
#
#     location /api {
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header Host $http_host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Nginx-Proxy true;
#
#         proxy_pass http://localhost:3000;  # Flask backend
#         proxy_redirect off;
#     }
# }